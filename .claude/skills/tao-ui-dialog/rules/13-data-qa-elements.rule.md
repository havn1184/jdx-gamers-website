# DLG-13 — data-qa trên phần tử tương tác

**Mức độ:** ERROR

Các input/button/select/combobox trong dialog phải có `data-qa` với tiền tố chuẩn: `i_` (input), `btn_` (button), `sel_` (select), `cmb_` (combobox) — phục vụ test tự động.
