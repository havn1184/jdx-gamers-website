# DLG-12 — SearchCombobox cho trường khóa ngoại

**Mức độ:** INFO

Trường foreign key (chọn 1 bản ghi liên kết) nên dùng component `SearchCombobox` thay vì `<select>` thường, để hỗ trợ tìm kiếm khi danh sách dài.
